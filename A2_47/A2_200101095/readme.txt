CS 348 (Implementation of Programming Languages Lab)
Assignment 2 

Name: Sidharth Choudhary
Rollno: 200101095

Remarks:

- "input.txt" should be present in the same directory as the C++ code, and must contain the assembly code
- "output.o" will contain the required machine code and will be generated at the end of the execution. 
- "intermediate.txt" and "listing.txt" files will also be generated as intermediates.
- Any error faced during the code execution will be printed in the terminal
- New instructions must begin in a new line
- Lines with no labels must have "*" before the opcode
- Lines with no operands must have "*" in place of the operand
- Comments are denoted by "." symbol in the Label field.
- Fields in the "input.txt" file must be separated by a single space (along a row)
- "SymTable.txt" and "OpTable.txt" files are generated in the same directory

Steps for running:

Compile using:
g++ 200101095_Assign02.cpp

Run using:
./a.exe
